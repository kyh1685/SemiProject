<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="EndlistClear">
  

<insert id="insert">
insert into endList_clear values(endList_clear_seq.nextval,2,#{buyer},#{goodWriter},#{title},#{price},sysdate,#{goodSeq})
</insert>
  <insert id="insertEndList_Clear">
  	insert into endList_clear values(endList_clear_seq.nextval,#{cateCode},#{buyer},#{goodWriter},#{goodName},#{price},sysdate,#{goodSeq})
  </insert>
  </mapper>
